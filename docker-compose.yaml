name: ascii_payment_system

services:
  mariadb:
    image: mariadb:11.8
    container_name: ascii_payment
    restart: unless-stopped
    environment:
      MARIADB_USER: admin
      MARIADB_PASSWORD: qazwsxedc
      MARIADB_ROOT_PASSWORD: qazwsxedc
      MARIADB_DATABASE: ascii_payment_db
      MARIADB_ROOT_HOST: "%"
    ports:
      - "3306:3306"
    volumes:
      - ascii_payment_vol:/var/lib/mysql
    networks:
      - ascii_payment_net

  postgres:
    image: postgres:17.6
    container_name: ascii_payment
    restart: unless-stopped
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: qazwsxedc
      POSTGRES_DB: ascii_payment
    ports:
      - "5432:5432"
    volumes:
      - ascii_payment_vol:/var/lib/postgresql/data
    networks:
      - ascii_payment_net

volumes:
  ascii_payment_vol:

networks:
  ascii_payment_net:
    driver: bridge
